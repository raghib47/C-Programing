#include<stdio.h>
int main(){

    int i=1,n,sum=0;
    float avg=0;

    printf("enter the value of n :");
    scanf("%d",&n);

    do
    {
        sum = sum+i;
        i = i+1;
    } while (i<=n);

    avg = sum/n;

    printf("avg = %f",avg);
    
}
